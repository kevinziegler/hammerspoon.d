-- hs.styledtext.setStyle({ font  = { name = "Overpass Mono", size = 18 } })
hs.styledtext.font = { name = "Overpass Mono", size = 18 }
hs.alert.defaultStyle.radius = 5

recursiveBinderFormat = {
    atScreenEdge = 0,
    padding = 20,
    radius = 5,
    textStyle = {
        font = {
            name = "Overpass Mono",
            size = 18
        }
    }
}

